{
    "version": "2.0.0",
    "command": "/opt/processing/processing-java",
    "tasks": [{
        "label": "1. Run processing sketch",
        "command": [
            "/opt/processing/processing-java",
            "--force",
            "--sketch=${fileDirname}",
            "--output=${fileDirname}/out",
            "--run"
        ],
        "type": "shell",
        "problemMatcher": [],
        "group": {
            "kind": "build",
            "isDefault": true
        }
    }]
}